// ? Общие переиспользуемые UI-типы;

/**
 * Направление отрисовки выпадающих списков `ListBox`, `Dropdown`;
 */
export type DropdownDirectionUnionType = 'top left' | 'top right' | 'bottom left' | 'bottom right';
