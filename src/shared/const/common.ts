// ? Здесь храним различные enum'ы;

// ? Список валют;
export enum CURRENCY {
    'RUB' = 'RUB',
    'EUR' = 'EUR',
    'USD' = 'USD',
}

// ? Список стран;
export enum COUNTRY {
    RUSSIA = 'Russia',
    BELARUS = 'Belarus',
    UKRAIN = 'Ukrain',
    KAZAKHSTAN = 'Kazakhstan',
    ARMENIA = 'Armenia'
}
